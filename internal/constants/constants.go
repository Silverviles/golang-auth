package constants

const (
	// QueryPath is the path to the file containing the SQL queries
	QueryPath = "internal/config/queries.txt"

	// EnvironmentVariablePath is the path to the .env file
	EnvironmentVariablePath = ".env"
)
